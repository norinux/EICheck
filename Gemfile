# frozen_string_literal: true
# run to '$ rbenv exec bundle install --path vendor/bundle'
source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'sinatra-config-file'
gem 'google_drive'
gem 'oauth2'

group :test do
  gem 'rspec'
  gem 'rack-test'
end
